//Function that takes in a boolean and converts in to string.

function boolToWord(bool) {
    return (bool ? "Yes" : "No");
}

boolToWord(true);
boolToWord(false);

// answer returns true = "Yes", false = "No".